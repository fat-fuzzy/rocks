// since there's no dynamic data here, we can prerender
// it so that it gets served as a static asset in production// Prerender on a per page basis
export const prerender = false

export const ssr = true
