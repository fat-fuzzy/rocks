export enum UiState {
	active = 'active',
	inactive = 'inactive',
	expanded = 'expanded',
	collapsed = 'collapsed',
}

export enum UiStatus {
	default = 'default',
	info = 'info',
	success = 'success',
	warning = 'warning',
	error = 'error',
}

export enum UiTextContext {
	form = 'form',
	prose = 'prose',
	code = 'code',
}

export enum ButtonEvent {
	expand = 'expand',
	collapse = 'collapse',
	toggle = 'toggle',
	switch = 'switch',
	click = 'click',
}
